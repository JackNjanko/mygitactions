name: Generate ASCII Artwork

on:
  push

jobs: 
  ascii_job:
    runs-on: ubuntu-latest
    steps:
    -name: checkout Repo
    uses: actions/checkout@v4

    -name: Repo file
    run: ls -ltra

    -name: Executing shell script
     run: |
       chmod +x
       ./ascii-script.sh